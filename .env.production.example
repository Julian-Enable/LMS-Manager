# PRODUCCIÓN - Railway Environment Variables
# ============================================

# CRITICAL: Genera una nueva SECRET_KEY para producción
# Usa: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=CHANGE_THIS_IN_PRODUCTION

# Debug DEBE ser False en producción
DEBUG=False

# Railway te provee DATABASE_URL automáticamente
# No necesitas configurarlo manualmente
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Dominios permitidos (Railway automáticamente te da uno)
# Formato: tu-app.up.railway.app,tu-dominio.com
ALLOWED_HOSTS=.up.railway.app,.railway.app

# CSRF Origins (usa tus dominios de Railway)
CSRF_TRUSTED_ORIGINS=https://tu-app.up.railway.app,https://tu-dominio.com

# SSL/HTTPS
SECURE_SSL_REDIRECT=True

# Admin URL personalizado (más seguro que /admin)
ADMIN_URL=panel-secreto/

# Configuración de Email (opcional)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-app-password
